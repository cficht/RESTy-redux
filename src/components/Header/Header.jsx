import React from 'react';
// import PropTypes from 'prop-types';

const Header = () => {
  return (
    <>
      <h1>RESTy Redux</h1>
    </>
  );
};

// Header.propTypes = {};

export default Header;
